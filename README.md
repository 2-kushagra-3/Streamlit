# Streamlit Cloud Starter

This repo contains a minimal Streamlit app ready for Streamlit Community Cloud.

## Run locally

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py
```

## Deploy

Push this repo to GitHub and deploy from Streamlit Community Cloud.
